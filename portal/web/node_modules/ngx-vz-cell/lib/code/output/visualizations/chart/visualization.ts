import {
    ComponentFactoryResolver,
    ComponentFactory
} from "@angular/core";

import {Visualization} from "../visualization.interface";
import {ChartComponent} from "./chart.component";

export class ChartVisualization implements Visualization {
    public component: ComponentFactory<any>; /*TODO: Figure out component factory type....*/
    constructor(private componentFactoryResolver: ComponentFactoryResolver) {
        this.component = componentFactoryResolver.resolveComponentFactory(ChartComponent);
    }
}
