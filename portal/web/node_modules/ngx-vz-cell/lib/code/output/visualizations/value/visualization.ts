import {ComponentFactory, ComponentFactoryResolver} from "@angular/core";
import {Visualization} from "../visualization.interface";
import {ValueVisualizationComponent} from "./value.component";

// Implement visualization interface for Values.
export class ValueVisualization implements Visualization {
    public component: ComponentFactory<any>; /*TODO: Figure out component factory type....*/


    constructor(componentFactoryResolver: ComponentFactoryResolver) {
        this.component = componentFactoryResolver.resolveComponentFactory(ValueVisualizationComponent);
    }
}
